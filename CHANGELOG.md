# سجل التغييرات — نظام أثير لإدارة الفنادق

كل تسليم يسجَّل هنا حسب قاعدة الحوكمة 7 (ملف 15) — الأحدث أولاً.

## [0.11.0] — 2026-08-06 — المرحلة 10.5: وحدة الغرف والأسعار + الأجنحة المركبة ✅
- **استدراك نطاق G2** (ملف 03 §1.1–1.5): إدارة كاملة بشاشات وواجهات API
  لأنواع الغرف (سعر الليلة الأساسي/سعة/أسرّة/مرافق)، خطط الأسعار (مرجع
  إضافي فوق الأساس/إفطار/سياسة إلغاء/أدنى ليالٍ/شركات)، تقويم الأسعار
  (تعبئة مدى ≤92 يوماً Upsert بالمفتاح نوع+خطة+يوم — تسلسل التسعير
  تقويم←خطة←أساس كما كان)، والخدمات الإضافية بحساب إيراد إلزامي.
- **الأجنحة المركبة (ADR-0035):** غرفة افتراضية قابلة للبيع
  `kind=SUITE_UNIT` بنوعها وسعرها + أبناء عبر `parent_room_id`؛ الحجب
  **مشتق** لا يُخزَّن: الجناح محجوز ⇐ كل أبنائه مرفوضون بالمدى؛ ابن
  محجوز ⇐ الجناح مرفوض وإخوته يبيعون؛ OOO يحجب الاتجاهين. نفس الحارس
  `HOTEL.SUITE_BLOCKED` على بحث التوفر/الإنشاء/التعديل/التسكين/النقل.
- حُرّاس الربط/الفك: لا جناح داخل جناح ولا حلقات، لا فك ربط بحجز نشط
  مستقبلي للأب — وإلغاء الحجز يحرر الطرفين لحظياً (لا حالة عالقة).
- API: POST/PATCH rooms (فئة+أب+مميزات+تفعيل)، أنواع/خطط/تقويم/خدمات
  CRUD بصلاحيات `rooms.manage`/`rates.manage`/`extras.manage` (الاستقبال
  يقرأ بلا إدارة — مختبَر 403)، كلها موثقة بالتدقيق.
- الترقية: `_ensure_additive_columns` يضيف العمودين غيابياً لقواعد
  قائمة؛ بذرة تجريبية غيابية: جناح ملكي J-401 (=401+402، سعر 300).
- المزامنة: حقلا الجناح ضمن حمولة الغرفة + رفع أولي **آباء أولاً** يحترم
  FK؛ وإعادة تعبئة صفوف FAILED ↔ RECEIVED تصحّح ثقوب الإرسال.
- الواجهة 🛏: خمسة تبويبات RTL (غرف بشارة 🏰 جناح ← أبناء/🔒 سبب حجب،
  أنواع، خطط، تقويم بمراجعة مدى وحذف قيد، خدمات) + نوافذ تعديل موجّهة
  + شارة الجناح براك الاستقبال.
- الاختبارات: 240/240 (17 جديداً `tests/test_suites.py`) — سيناريوها
  البيع التوأم مؤكد العميل، OOO الاتجاهين، حارس الفك، التدقيق الليلي
  بسعر الجناح، Upsert التقويم، RBAC، وحمولة المزامنة.

## [0.10.0] — 2026-08-06 — المرحلة 10: محرك المزامنة الهجينة (بوابة G10) ✅
- `backend/app/synck.py`: محرك مزامنة كامل — التقاط Outbox ذرّي داخل
  معاملة العمل (قيود بخطاف الترحيل + ماسترات بأحداث Mapper)، دفعات ≤500
  موقعة Ed25519، Inbox خام فريد (site,seq) بACK متصل، عقد مخطط بحجر
  آمن حتى الترقية، LWW بنسخ معلومة + صندوق تعارض بحسم بشري موثق +
  Tombstone، Anti-Entropy بمقارنة العدادات، Reseed كامل من الأرشيف،
  USB مشفر (Fernet/PBKDF2-120k + فاحص عبث SHA-256 + توقيع الموقع)،
  ورفع أولي Backfill موثق وآمن التكرار (ADR-0034).
- `backend/app/api/sync.py`: طرف الموقع (status/pair/push-now/lean/
  backfill/conflicts/USB/pull-licenses/heartbeat) بRBAC كامل + طرف
  المستقبِل (receive/counts/register/reseed/inbox) بوضع SYNC_RECEIVER.
- الجداول: sync_site/events_out/events_in/conflicts/entity_versions/
  site_regs (تتبع النسخ خارج الجداول التجارية — صفر تعديل عليها).
- شاشة «مركز المزامنة» 🔄 بالواجهة: حالة حية كل 15ث، دفع فوري، رفع أولي،
  وضع توفير، سحب تراخيص، نبضة، USB، صندوق تعارضات جنباً-إلى-جنب، جدول
  الأحداث.
- **اختبار الفوضى الإلزامي ×1000 (قبول §7-1): صفر فقدان، صفر ازدواج،
  فجوات ≤1 مستدركة، وميزانا الطرفين متطابقان** + 16 اختباراً آخر يغطي
  كل معايير §7 (223/223 إجمالاً).
- إصلاحات بنيوية أثناء التصليب: قيم الحمولة JSON-آمنة (Decimal نصاً)،
  تطبيق الماسترات داخل SAVEPOINT، مقارنة LWW بأنواع متسقة، created_at
  نظامي عند الإدراج المزامَن، ContextVar الكبت بنمط token (لا تسريب).

## [0.9.0] — 2026-08-05 — المرحلة 9: لوحة تحكم الشركة المالكة (بوابة G9) ✅
- تطبيق مستقل `backend/vendor/` (منفذ 8100، قاعدة وسرّ JWT خاصان، بيئة
  VENDOR_*) — عزل كامل عن أنظمة العملاء وصفر ثقة عكسية (ملف 08 §1).
- CRM عملاء بملف كامل + سجل حالات إلزامي السبب/المستخدم/الطابع (قبول
  §8-5؛ السبب الفارغ مرفوض) + خطط أسعار بسقف خصم اعتماد.
- دورة فوترة: فاتورة بتذكير 30/14/7 ← تحصيل يدوي موثق بمرجع ← **توليد
  ترخيص مجدد آلياً عند اكتمال السداد** وتحديث ملف العميل مرة واحدة ←
  قواعد: انتهاء بلا سداد ← GRACE + مهمة مبيعات ← READ_ONLY ذاتية.
- تراخيص: إصدار/تجديد/تعديل موقَّع بنفس سلطة 07؛ تجاوز الخطة ← اعتماد
  ثانٍ إلزامي (لا اعتماد ذاتي)؛ قوائم إلغاء موقعة متسلسلة تُسلَّم عبر Edge.
- تذاكر: SLA محسوب 4/24/72/168س، سير عمل ستّي حرفي، سبب جذري إلزامي
  لإغلاق الأعطال، تصعيد L1←L2←هندسة، و**P1 يُشعِر المناوب < 60ث (§8-3)**.
- صحة أسطول موحدة (نبض/إصدار/Outbox/نسخ/قرص ← أخضر/أصفر/أحمر/بلا-نبض)،
  حلقات تحديث Canary بسجل نتائج، وعلم بصمة جهاز على Tenant آخر (07 §4-5).
- أمن: MFA إلزامي بلا استثناء مع تسجيل إجباري أول دخول؛ أقل امتياز
  (دعم لا يصدر تراخيص — قبول §8-4؛ مالية لا تعمل تذاكر)؛ تدقيق مستقل
  مسلسل على كل فعل حساس.
- واجهة العميل المحدودة /edge/* برمز العميل: نبض + أحدث ترخيص موقّع
  + أحدث قائمة إلغاء + فتح تذكرة + أحدث إصدار وإقراره — لا أكثر أبداً.
- MRR/ARR/تجديد/Churn/أعمار تأخر/متوقع 90ي بدقة Decimal مع إثبات MRR
  تفصيلي (قبول §8-2 بلا انحراف).
- واجهة ويب RTL مضمّنة (تسجيل MFA مرئي، 7 تبويبات) + سكربت تشغيل
  scripts/run_vendor.sh + أداة vendor_totp.py للمعاينة التطويرية.
- الاختبارات 186 → 206 (20 جديدة تغطي معايير §8 الخمسة كاملة).

## [0.8.0] — 2026-08-05 — المرحلة 8: نظام التراخيص كاملاً (بوابة G8) ✅
- توقيع Ed25519 محلي التحقق (07 §1): المفتاح العام مضمَّن، الخاص خارج
  المستودع (dev_keys/ مهمَل) + أداة الشركة `backend/scripts/license_tool.py`
  (توليد مفاتيح/إصدار/قوائم إلغاء) + فحص آلي يمنع تسرب مفتاح خاص (§7-5).
- ملف ترخيص JSON موقَّع على الحقول كاملة مع serial رتيب ضد التراجع،
  وبصمة جهاز 3 مكونات مجزأة بتسامح 2/3 (§2)، وتفعيل معزول تماماً بطلب
  بصمة ثم ملف مقيَّد من الشركة.
- دورة حياة رحيمة (§3): GRACE كامل الوظائف ← READ_ONLY (تقارير/طباعة/
  تصدير سليمة؛ الإضافات فقط تُمنع بـ LIC.READ_ONLY) ← SUSPENDED حصرياً
  بوثيقة SUSPEND موقعة من الشركة (لا إيقاف آلي في الكود) وعندها لا دخول
  إلا لحساب الطوارئ المالي للتصدير (auth.ensure_login_allowed).
- مكافحة عبث (§4): مرساة ساعة رتيبة — رجوع > يوم = CLOCK_LOCK بلا فقدان
  يفك بملف إصلاح موقّع يعيد الترسيخ؛ رفض فوري لأي تعديل (§7-1)؛ اكتشاف
  بصمة أخرى = INVALID + LICENSE_TAMPER_SUSPECT؛ قوائم إلغاء موقعة
  بتسلسل متزايد فقط.
- القيود الصلبة (§5): حارس require_module على موجّهات HOTEL/POS/INVENTORY/
  HR/ASSETS برسالة ترقية LIC.MODULE_DISABLED، وحد مستخدمين عند الإنشاء
  LIC.USER_LIMIT، والقراءة دوماً من الحمولة الموقّعة.
- تسجيل (§6): أحداث LICENSE_* كلها في سلسلة التدقيق التجزئية (تثبيت/فشل
  تحقق/فحص دوري 6 ساعات كسول/إلغاء/عبث/تغيير حالة/طلب تفعيل).
- وضع TRIAL كامل المزايا 90+30 يوماً للتثبيت بلا ملف (ADR-0032).
- واجهة: شاشة /license كاملة (حالة/حدود بعدادات/وحدات/بصمة/تثبيت لصقي/
  طلب تفعيل/قوائم إلغاء/فحص فوري) + شريط تنبيه رحيم بالهيكل (أصفر مهلة،
  أحمر قيد، كل 5 دقائق).
- الاختبارات 168 → 186 (18 جديدة تغطي معايير قبول §7 الخمسة كلها عبر
  خدمة وHTTP)؛ تجربة حية: ترخيص LIC-2026-0001 مقيَّد بالبصمة مثبَّت على
  العرض التجريبي حتى 2027-08-05 وملف معبّث مرفوض فوراً.

## [0.7.0] — 2026-08-05 — المرحلة 7: الأصول الثابتة + إقفال السنة + القوائم (بوابة G7) ✅
- الأصول الثابتة (ملف 11 (ز)): سجل بتكلفة/خردة/عمر وطريقتي إهلاك (قسط
  ثابت بالهللة + متناقص مضاعف بأرضية خردة مطلقة)، بدء من الشهر التالي
  للشراء، إعدام موثق يوقف الشحن، وجدولة استشرافية 12 شهراً لكل أصل.
- حدث #24 حياً: تشغيلة شهرية واحدة فقط لكل شهر (Dr 7101/حساب الأصل ←
  Cr 1590/مجمّعه عبر المحرك ذرياً) بلقطة سطور صندوقة، وإعادة الشهر
  مرفوضة على ثلاث طبقات (قيد فريد + آخر شهر لكل أصل + Outbox).
- تقرير «سجل ≡ أستاذ»: NBV السجل يطابق (حسابات الأصول − المجمعات)
  مع علم مطابقة يكشف أي أصل بلا قيد اقتناء موثق (ADR-0030) — وقيود
  الاقتناء الافتتاحية للأصول المزروعة تطبقه فعلياً.
- حالات الفترات بصيغة 02 §5 الكاملة (استكمال ADR-0028): لين (يوقف
  العادي بفحص لا-مسودات وصفرية 8001) ← صلب نهائي، وقيد تسوية للمدير
  المالي عبر منفذ مستقل بصلاحية periods.adjust وبصمة سرد مميِّزة.
- إقفال السنة آلي (02 §5): قائمة تحقق حية ← قيد CLOSING واحد متوازن
  (إيرادات↔3202↔مصروفات والصافي إلى 3201 حرفياً بالنص) ← سنة جديدة
  بـ12 فترة ← قيد OPENING بكل موازين الميزانية و**بأرصدة كل طرف** ←
  سنة قديمة موصدة بكل فتراتها صلبة، وإعادة التنفيذ 400 بمفاتيح Outbox.
- بنية المرساة (ADR-0031 — حل جذري لمشكلة الدفتر المستمر): تجميع
  موازين التقارير يبدأ من آخر افتتاحية فلا تتضاعف الأرصدة عند عبور
  السنة، والميزان/الأستاذ/الأعمار/سجل الأصول يرثها كلها من نقطة واحدة.
- القوائم المالية كاملة (02 §10): قائمة دخل USALI قسمية (دخل قسم ←
  غير موزعة ← GOP ← صافٍ)، ميزانية بقطع متوازنة بضبط داخلي، تدفقات
  نقدية غير مباشرة بهوية Δ نقدية مقفلة بالهللة، وأعمار ذمم FIFO
  (0-30/31-60/61-90/+90) للنزلاء والمدينة والموردين تطابق الأستاذ حرفياً.
- **الاختبارات: 168/168 ✅ (+22 لبوابة G7) — قبول 7/1..7/4: قسط موقَّع
  250.0000 ومتناقص حتى أرضية الخردة، تشغيلة #24 متوازنة مرة واحدة،
  USALI ذهبية (غرف 7000 / F&B 1700 / GOP 8200 / صافٍ 7950)، ميزانية
  balanced دائماً، هوية نقدية بفارق صفر، أعمار=أستاذ بالتخصيص، إقفال
  يصفّر الإيراد/المصروف و3201 يستلم 7000 بالضبط والافتتاحية بحزبية سلفة 500.**
- تحقق حي: تشغيلة يوليو 544.7917 لثلاثة أصول، سجل ≡ أستاذ 24955.2083،
  RUN_EXISTS عند الإعادة، شاشات /fin/* الثلاث حية 200.
- واجهات RTL جديدة: 🏭 الأصول الثابتة (سجل/جدولة/تشغيلات)، 🧾 القوائم
  المالية الأربع، 🔐 الإقفال والفترات (خريطة فترات + معالج سنة ذري).
- قرارات جديدة: ADR-0028 (لين/صلب/تسوية) · ADR-0029 (اتفاقيات الإهلاك)
  · ADR-0030 (اقتناء يدوي موثق) · ADR-0031 (افتتاحية مرسية).

## [0.6.0] — 2026-08-05 — المرحلة 6: الموارد البشرية والرواتب (بوابة G6) ✅
- هيكل توظيف كامل: أقسام بمراكز تكلفة وحسابات مصروف GL، وظائف ودرجات، ملفات
  موظفين بهوية مشفرة ساكناً وبدلات ثابتة، دورة حياة (تعليق/إنهاء) وخطابات
  تغيير بمستند واعتماد غير ذاتي وأثر رجعي محمي (ملف 06 §1).
- الوقت والحضور §2: ورديات (ليلية عابرة مدعومة) وجداول Roster شهرية؛ رصد حضور
  يدوي **لا يُحتسب إلا باعتماد المشرف** مع تتبع غير المعتمد وحجب فك الاعتماد
  بعد ترحيل الشهر؛ إجازات بأنواع واستحقاق شهري وخصم رصيد آلياً مع حجز المعلق
  ومنع التجاوز؛ جزاءات تأديبية بمستند واعتماد ودائن قابل للسياسة (2220/4901).
- السلف §3: سقف % من الراتب مفروض خادمياً → اعتماد → صرف بقيد #23 (1130 طرف
  EMPLOYEE ← نقدية) → جدولة إقساط شهرية آلية من أول راتب، واستحالة خصمٍ فوق
  المتبقي واستحالة صرفٍ مؤصل مكرر (مفاتيح Outbox لكل خطوة).
- محرك مسير §4: بنود قابلة للضبط (ثابت/% من الأساس) وخصومات قانونية بائتة
  تُهيَّأ لكل بلد؛ معالج شهري بلقطة Snapshot كاملة للبنود والمدخلات بدءاً من
  معتمدات الشهر فقط؛ اعتماد مزدوج صارم (المُعِدّ HR ≠ المعتمد مالية، والذات-
  اعتماد مرفوض برسالة عربية)؛ ترحيل #21 ذرياً ومتوازناً بالبِنية موزعاً على
  حسابات الأقسام وصافيه = 2310؛ صرف #22 جماعي/فردي بتسلسل إثباتات؛ مسير واحد
  نشط شهرياً وتسويات SUPPLEMENTAL بأرقام محجوزة بلا فجوات تظهر سطوراً جديدة
  بكشف الموظف؛ مسير FINAL فوري للمنتهي بمكافأة وتسوية سلف؛ مخصص نهاية خدمة
  شهري اختياري بقيد مستقل (2320 قابل للضبط — ADR-0027).
- الخصوصية الصفية §6: تقنيع الرواتب في القوائم، منع صارم للقسائم، أقسام سرية
  🔒 فوق RBAC بتفويض صريح، وأي فتح قسيمة حدث مُدقَّق (ADR-0025).
- التقارير §5: كشف شهري قسم/موظف/بند، مقارنة أشهر برسم شريطي، تقادم سلف
  (كشف 1130)، أرصدة إجازات مستحقة، دوران موظفين، وكلفة الموظف الكاملة مقابل
  إيراد الشهر (مؤشر USALI استرشادي بحدّ 40%).
- قرارات جديدة: ADR-0023 (غير المكتسب يخفض المصروف داخل #21)، ADR-0024 (بادئة
  hr_ وترقيم بلا فجوات)، ADR-0025 (نموذج الخصوصية الثلاثي)، ADR-0026 (نطاق
  v1 المحسوب)، ADR-0027 (دائن المخصص 2320).
- **الاختبارات: 146/146 ✅ (+28 لبوابة G6 تغطي معايير قبول ملف 06 §7 الأربعة
  حرفياً: عينة ذهبية موقَّعة 3656.2500/2861.2500، توزيع #21 وصافيه=2310،
  استحالات السلفة الزائدة وراتب المنتهي والصافي السالب، وثبات المعتمد
  وتسوية-كسطر-جديد).** تحقق حي: دورة 2026-03 مكتملة على الخادم بقيد متوازن
  Dr=Cr=8250.0000 ورفض ذات-اعتماد موثق.
- واجهة عربية RTL: 4 شاشات جديدة (👥 شؤون الموظفين، ⏰ الوقت والحضور، 💰
  الرواتب والمسيرات، 📈 تقارير التوظيف) بتبويبات معالجية وصلاحيات شرطية.

## [0.5.0] — 2026-08-05 — المرحلة 5: المخزون والمشتريات (بوابة G5) ✅
- كتالوج مخزون كامل: تصنيفات (متوسط مرجح متحرك؛ FIFO محجوز ADR-0018)، أصناف
  بوحدات بديلة بمعاملات إلزامية وحدي طلب/أمان وتتبع دفعات-صلاحية، مستودعات
  رئيسي/فرعي/منفذ بحسابات 12xx وأمناء، موردون بأسعار تعاقدية مؤرخة وتقييم.
- دورة مشتريات State Machine بسلّم اعتماد L0/L1/L2 (ملف 14 §2، بلا اعتماد ذاتي):
  PR → PO (معامل وحدة إلزامي وسعر تعاقدي افتراضي) → GRN (سقف كمية % ورفض جودة
  وصلاحية إلزامية واستحالة تعديل المرحَّل من كل المسارات — عكس موثق فقط).
- مطابقة ثلاثية PO↔GRN↔Invoice: إيقاف آلي خارج التسامحات، حلّ بتعديل/اعتماد فرق
  يُرحَّل لحساب فروقات أسعار الشراء 5120 (يُزرع مع ترقية الدليل لقواعد قائمة).
- سداد موردين #16 (خصم مكتسب دائناً 4901) ومرتجعات #17 (Dr 2101/1101 ← Cr مخزون).
- حركة: صرف أقسام #18 (بلا أثر دخل)، تحويلات بمسوغ و«على الطريق» باستلام مزدوج
  (المستلم ≠ المرسل على الخادم)، هالك #19 بمستند واعتماد وإحصائية شهرية.
- جرد دوري: تجميد نطاق يمنع كل حركة ⇐ عدّ ⇐ فروقات ⇐ اعتماد مزدوج مفروض
  (L1≠L2≠المنشئ) ⇐ تسويات #19/#20 بالهللة؛ زيادة بلا فقاعة متوسط (ADR-0022).
- جسر POS (ADR-0017): مستودع آلي لكل منفذ وترحيل `pos_stock` القديم إلى دفتر
  مخزون الوحدة بلا قيد مكرر — القيمة مطابقة يوم الهجرة وتبقى (اختبارات + حي).
- تنبيهات (إعادة طلب باقتراح PO آلي/صلاحية 30-15-7/راكد 90) وفحص يلي مطابقة
  مخزون=أستاذ ينبه عند أي فرق؛ تقارير أرصدة/مشتريات/أداء موردين/فروقات أسعار/
  هالك/استهلاك نظري↔فعلي؛ سياسة مستأجر قابلة للضبط موثقة بالتدقيق.
- استحالة الرصيد السالب بتحديث ذري محروس (إثبات تزامني بخيطين: واحد ينجح فقط)
  ووضع «سماح سالب» اختياري صريح لكل مستودع.
- API: ~55 نقطة `/api/inv` بمخططات مدخلات وRBAC ورسائل عربية ومرجعية أكواد INV.*.
- واجهات RTL: كتالوج المخزون، دورة المشتريات (5 تبويبات + محرر بنود وتعبئة سريعة)،
  حركة المخزون (صرف/تحويل/هالك/جرد بشبكة عدّ)، تقارير وتنبيهات وسياسة (6 تبويبات).
- 46 اختباراً جديداً (شاملاً إثبات التزامن والسالبة والمطابقة وE2E كاملاً عبر HTTP):
  إجمالي المجموعة **118/118 ✅**.
- قرارات: ADR-0017..0022 في docs/DECISIONS.md.

## [0.4.0] — 2026-08-05 — المرحلة 4: نقاط البيع (بوابة G4) ✅
- منافذ بيع (REST/CAFE) بصندوق وسياسة مخزون وتسلسل مستقل لكل منفذ.
- كتالوج كامل: تصنيفات/أصناف بثلاثة أنواع/معدِّلات مقيدة/وصفات مركبة.
- دورة طلب State Machine + تسديد Split بكل الوسائل وشحن غرفة وشركة ومجاني مصرّح.
- #13 خصم وصفة حرفي + مخزون كمّي بسجل حركة إلحاقي (ممنوع السالب افتراضياً).
- ورديات + Z-Report أرشيفي موقّع + فرق عهدة مقيد ضمن تسامح المنفذ.
- متانة الشبكة: client_uuid + طابور متصفح تلقائي المزامنة (لا فقدان فاتورة).
- PIN اعتماد فوري للمدير (argon2) للخصومات فوق الحد وHouse Use.
- واجهات: شاشة بيع لمسية (اختصارات F2/F9 ودعم باركود)، طلبات وKDS، ورديات، كتالوج، تقارير.
- 26 اختباراً جديداً يغطي معايير القبول الخمسة حرفياً ⇐ **72/72 ✅**.
- قرارات: ADR-0012..0016 في docs/DECISIONS.md.

## [0.3.0] — 2026-08-04 — المرحلتان 2+3: الفندق والتدقيق الليلي (G2+G3) ✅
- حجوزات State Machine كاملة + استحالة حجز مزدوج (إثبات تزامني بخيطين).
- فوليو مشتق من الدفتر لحظياً (لا رصيد مخزن) + شحنات/دفعات/خصومات/نوافذ شركات.
- تسكين ≤60ث مع قاعدة الغرفة النظيفة، Walk-in، نقل غرفة، مغادرة بفاتورة رسمية.
- تدقيق ليلي ذري مُقفَّل يتقدم بتاريخ العمل + Snapshot إشغال/ADR/RevPAR.
- هوسكيبينج بمصفوفة صلاحيات، OOO/OOS يحجب البيع وينذر عند تعارض حجز.
- تشفير هوية النزيل ساكناً (Fernet) + عرض مقنّع.
- إصلاح سلسلة التدقيق المتوازية — كشفه نظامنا نفسه (ADR-0011).
- 26 اختباراً جديداً ⇐ **46/46 ✅**.

## [0.1.0] — 2026-08-04 — المرحلتان 0+1: التأسيس والنواة المحاسبية (G1) ✅
- FastAPI + SQLAlchemy 2 + React RTL (قاعدة كود واحدة: محلي/سحابة/هجين).
- محرك قيود مزدوجة: V1..V8، ترحيل ذري، عكس فقط للتصحيح، لا تعديل للمرحَّل.
- دليل حسابات فندقي كامل من ملف 02 §2.2 + خريطة ربط §8 في قاعدة البيانات.
- سلسلة تدقيق مقاومة للتلاعب (سلسلة تجزئة) + فاحص سلامة + RBAC كامل.
- ميزان مراجعة ودفتر أستاذ وشهر G1 كامل ⇐ **20/20 ✅**.
