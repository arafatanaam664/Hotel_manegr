# لوحة حالة المشروع — نظام أثير للضيافة

آخر تحديث: **2026-08-05** · الفرع: `arena/019fcd85-hotel-manegr`

## المرحلة 0 — الأساس ✅
- هيكل المستودع: `backend/` (FastAPI + SQLAlchemy2) ،`frontend/` (React+TS+Vite — قيد البناء)، `docs/`، `study/`، `.github/workflows/`
- إعدادات بيئية (`config.py`) بنمط واحد local/cloud/hybrid عبر متغيرات
- سجل قرارات ADR مفعّل (انظر DECISIONS.md)

## المرحلة 1 — نواة المحاسبة (بوابة G1) ✅
| البند | الحالة |
|---|---|
| نماذج قاعدة البيانات (17 جدولاً، UUID، Numeric(19,4)) | ✅ |
| دليل حسابات فندقي كامل (~90 حساباً) من ملف 02 §2.2 | ✅ زرع أولي |
| محرك الترحيل: تحقق V1–V8، ذرية المعاملة، ترقيم مقفل | ✅ |
| خريطة الربط المحاسبي (10 أحداث تشغيلية) | ✅ |
| ثبات المرحَّل: لا PUT/PATCH/DELETE — عكس فقط | ✅ مختبَر |
| Idempotency (Outbox + event_key) | ✅ مختبَر |
| RBAC: 6 أدوار، صلاحيات دقيقة، رفض افتراضي | ✅ مختبَر |
| المصادقة: Argon2id، JWT وصول/تحديث، دوران عائلي + كشف سرقة | ✅ مختبَر |
| قفل الحساب (5 محاولات ← 10 دقائق) | ✅ مختبَر |
| سجل تدقيق بسلسلة تجزئة SHA-256 + كاشف تلاعب | ✅ مختبَر |
| ميزان مراجعة + دفتر أستاذ + فحص الصفريّة الصافي | ✅ |
| إغلاق الفترة الشهرية + منع الترحيل بعده | ✅ مختبَر |
| **سيناريو شهر فندقي كامل (G1): متوازن يومياً ونهائياً** | ✅ مختبَر |
| الاختبارات: 20/20 ناجح | ✅ |

## المرحلة 2 — العمليات الفندقية (بوابة G2) ✅
| البند | الحالة |
|---|---|
| الكيانات: غرف/أنواع/خطط أسعار/تقويم أسعار/خدمات/نزلاء (هوية مشفرة ساكناً)/شركات | ✅ |
| دورة الحجز State Machine حرفية + قفل تفاؤلي | ✅ مختبَر |
| منع الحجز المزدوج (قفل صف + فحص تداخل، اختبار تزامن بخيطين) | ✅ مختبَر |
| Snapshot سعر ليلة (المنقضية بسعرها القديم) + تسلسل تسعير: تقويم←خطة←أساس | ✅ مختبَر |
| الفوليو بلا رصيد مخزن — يُشتق من 1110 بالطرف لحظياً | ✅ مختبَر |
| الأحداث #01–#08 + مغادرة متأخرة + خصم معتمد + رد فائض + تحويل نوافذ | ✅ مختبَر |
| تسكين Walk-through + استثناء الغرفة غير النظيفة بصلاحية | ✅ مختبَر |
| مغادرة بفحوص مرتبة + فاتورة ضريبية بتسلسل رسمي + منع إعادة فتح | ✅ مختبَر (تطابق بالهللة) |
| نقل نزيل (شريحة جديدة، القديمة Dirty تلقائياً) | ✅ مختبَر |
| الهوسكيبينج بمصفوفة RBAC (طابق/مشرف) + تعطيل يستبعد من البيع + تنبيه تعارض | ✅ مختبَر |
| تقارير: إشغال/ADR/RevPAR، Pickup 30/60/90، أعمار العربون، In-House بأرصدة | ✅ |
| خصم فوق 100 يتطلب معتمداً (مسجل في التدقيق) | ✅ مختبَر |

## المرحلة 3 — التدقيق الليلي (بوابة G3) ✅
| البند | الحالة |
|---|---|
| تاريخ عمل يتقدم يوماً واحداً بإتمام التدقيق فقط | ✅ |
| فحوص الوصولات/المغادرات قبل الإقفال + حجب عند وجود معلق | ✅ مختبَر |
| ترحيل ليالٍ ذري (الكل أو لا شيء) بمفاتيح idempotent — استحالة ليلة مكررة | ✅ مختبَر |
| **G3: يومان متتاليان بلا تدخل** | ✅ مختبَر |
| Snapshot موقّع: إشغال/ADR/RevPAR + أرصدة صناديق النظام + تنبيه OOO متعارض | ✅ |
| ترقية قاعدة قائمة دون فقدان (ensure_hotel_upgrade) | ✅ |
| إصلاح سلسلة التدقيق المتوازية (flush إلزامي) — ADR-0011 | ✅ مختبَر انحداراً |

**الاختبارات الآن: 46/46 ✅**

## الواجهة الأمامية ✅ (محدّثة بالفندق + POS + المخزون)
استقبال براك لوني لحظي، حجوزات (إنشاء بفحص توفر + Walk-in)، ملف حجز كامل،
تدقيق ليلي، نزلاء وشركات — يضاف الآن: شاشة بيع لمسية RTL كاملة بطابور أوفلاين،
لوحة طلبات وشاشة تحضير KDS، ورديات وتقارير Z، كتالوج ووصفات ومخزون،
تقارير مبيعات بالأبعاد الستة + مؤشرات الرقابة — ومع G5: كتالوج مخزون
(أصناف/تصنيفات/مستودعات/موردون وأسعار تعاقدية)، دورة مشتريات كاملة
(طلبات/أوامر/استلامات/فواتير بمطابقة ثلاثية/سداد ومرتجعات)، حركة مخزون
(صرف/تحويلات باستلام مزدوج/هالك/جرد بتجميد وشبكة عدّ واعتماد مزدوج)،
وتقارير المخزون والتنبيهات والسياسة القابلة للضبط.


## المرحلة 4 — نقاط البيع POS (بوابة G4) ✅
| البند (ملف 04) | الحالة |
|---|---|
| منافذ بيع: صندوق نقد فرعي + سياسة مخزون سالب لكل منفذ + تسلسل فواتير مستقل | ✅ |
| كتالوج: تصنيفات ملونة بمحطة تحضير/أصناف (مخزوني/خدمي/مركّب)/معدِّلات مقيدة بالصنف | ✅ |
| طلبات: مسودة بلا أثر → Fire (توجيه مطبخ/بار) → إلغاء بند مشرف+سبب+Audit | ✅ |
| تسديد Split بكل الوسائل: نقد/بطاقة/محفظة/**على الغرفة**/**على شركة**/**مجاني مصرّح PIN** | ✅ مختبَر |
| أحداث #10..13: قيد بيع مركب بالمحرك + #13 خصم وصفة حرفي + Dr1210/Cr8002 افتتاحي | ✅ مختبَر |
| شحن على الغرفة: غرفة مشغولة فقط + سقف الفوليو + ظهور فوري بالفوليو + إلغاء من POS فقط بعكسي | ✅ مختبَر |
| House Use: Dr 6610 باعتماد PIN + سبب موثق باسم المعتمد | ✅ مختبَر |
| ورديات: لا فتح قبل إقفال، طلبات مفتوحة تحجب الإقفال، فرق عهدة ضمن التسامح ← CASH_OVER/SHORT | ✅ مختبَر |
| Z-Report أرشيفي موقّع غير قابل للتعديل (لا يوجد أي مسار تعديل في النظام) | ✅ |
| خصم فوق حد الدور (50) = اعتماد مدير فوري PIN مخزن argon2 | ✅ مختبَر |
| مرتجع بعد التسديد: فاتورة RETURN منفصلة + عكس بيع وتكلفة كامل + الأصل لا يُمس | ✅ مختبَر |
| **قبول #1:** بيعة نقدية كاملة أقل بكثير من 15 ث + قائمة مستقرة مع 3,000+ صنف مصفحة | ✅ مختبَر |
| **قبول #2:** محاسبة POS = تقرير المبيعات = زاوية القيود #10..13 (اختبار ثلاثي التطابق للفترة نفسها) | ✅ مختبَر |
| **قبول #3:** مركّب يخصم مكوناته بالضبط (BUN/BEEF/CHEESE/FRIES −3 لكل 3 برجر) | ✅ مختبَر |
| **قبول #4:** انقطاع الشبكة: client_uuid UNIQUE + إعادة إرسال = نفس الفاتورة بلا قيد مكرر + طابور متصفح يزامن تلقائياً | ✅ مختبَر |
| **قبول #5:** كل إلغاء/خصم/مرتجع بمستخدم + سبب + يظهر بالتدقيق وتقرير الرقابة | ✅ مختبَر |
| واجهة: شاشة بيع لمسية (F2/F9)، طلبات+KDS، ورديات+Z، كتالوج+وصفات+مخزون، تقارير بالأبعاد الستة | ✅ |
| RBAC: كاشير لا يلغي ولا يقفل كتالوجاً (SoD) | ✅ مختبَر عبر HTTP |

**الاختبارات الآن: 72/72 ✅ (26 جديداً للبوابة G4)**
تجربة حية على الخادم: فاتورة REST-2026-000001 بيع نقدي 20 (تكلفة 7.75 مقيدة #13)،
إعادة إرسال آمنة (replayed)، شحنة غرفة 101 بالفوليو فوراً، Z متوازنة 0.0000،
ميزان مراجعة متوازن وسلسلة تدقيق سليمة (24 سجلاً) — كلها منفذة فعلياً 2026-08-05.

## المرحلة 5 — المخزون والمشتريات (بوابة G5) ✅
| المعيار (ملف 05) | الحالة |
|---|---|
| كتالوج: تصنيفات بأسلوب تقييم (متوسط مرجح متحرك — FIFO محجوز مسبقاً)/أصناف بوحدات بديلة بمعامل إلزامي وحدي طلب وأمان وباركود وحساب 12xx ودفعات-صلاحية | ✅ مختبَر |
| مستودعات رئيسي/فرعي/منفذ POS — كل بمخزون مالي وأمين وعهدة، ومنع/سماح سالب لكل مستودع | ✅ مختبَر |
| موردون: شروط سداد وأسعار تعاقدية مؤرخة وتقييم أداء وحساب 2101 طرفه | ✅ مختبَر |
| دورة (#14..17): PR اختياري → PO بسلّم اعتاد L0/L1/L2 (لا اعتماد ذاتي — زر موقّع) → GRN مقابل PO أو مباشر (سقف كمية %، رفض جودة بسبب، صلاحية إلزامية للمتتبع، فاتورة مرفقة إلزامية للآجل) → مطابقة ثلاثية → سداد بخصم مكتسب 4901 → مرتجع #17 | ✅ مختبَر |
| **مطابقة ثلاثية PO↔GRN↔Invoice:** أي فرق سعر فوق التسامح أو كمية تتجاوز المستلَم المقبول يوقف الاعتماد آلياً (VARIANCE_HOLD) — حلّ بتعديل أو اعتماد فرق صلاحي يرحّل لحساب 5120 | ✅ مختبَر |
| صرف لقسم #18 (بلا أثر دخل — قيد فقط عند اختلاف الحسابات)، تحويل بمسوغ و«على الطريق» باستلام مزدوج (المستلم ≠ المرسل مفروض على الخادم) | ✅ مختبَر |
| هالك #19: مستند + سبب + مرجع صورة + اعتماد (ذات-الاعتماد ممنوع) + إحصائية شهرية | ✅ مختبَر |
| جرد دوري: تجميد فوري للنطاق (كل حركة ممنوعة حتى الإقفال/الإلغاء) → عدّ → فروقات → اعتماد مزدوج L1≠L2≠المنشئ → تسويات #19/#20 بالهللة | ✅ مختبَر |
| متوسط مرجح متحرك يُعاد آلياً عند كل إدخال؛ تكلفة لحظة البيع تخزَّن على سطر الفاتورة (إعادة البيع والمرتجع يرجعان بنفس التكلفة المخزنة — إعادة رجعية ممنوعة) | ✅ مختبَر |
| جسر POS (ADR-0017): مستودع آلي لكل منفذ، خصم الوصفات من محرك المخزون، رصيد POS يقرأ من دفتر مخزون الوحدة، رصيد الافتتاح يُرحَّل محلياً بلا قيد مكرر | ✅ مختبَر |
| تنبيهات: أقل من حد الإعادة (اقتراح PO = العجز حتى الأمان + متوسط استهلاك 30 يوم)/صلاحية 30-15-7/راكد 90 يوماً + مطابقة يومية مخزون=أستاذ ينبه عند أي فرق | ✅ مختبَر |
| تقارير: أرصدة قيمية تطابق 12xx، مشتريات مورد/فترة، أداء موردين، فروقات أسعار (5120)، هالك شهري، استهلاك نظري (وصفات) مقابل فعلي = مؤشر الهدر/التلاعب | ✅ مختبَر |
| **قبول #1:** استحالة رصيد سالب بوضع «المنع» + إثبات تزامني: خيطان يخصمان 6+6 من رصيد 10 ⇐ واحد ينجح فقط والرصيد ينتهي 4 | ✅ مختبَر |
| **قبول #2:** قيمة مخزون أي يوم = رصيد حساب الأستاذ (مطابقة آلية بعد عمليات مختلطة + فحص يكتشف قيداً يدوياً على 1210) | ✅ مختبَر |
| **قبول #3:** الثلاثية ترفض تلقائياً خارج التسامح (سعر/كمية/غير موجود بالأمر) | ✅ مختبَر |
| **قبول #4:** تعديل GRN مرحَّل مستحيل من كل المسارات: حارس الخدمة على أي مسار كودي + لا يوجد مسار PUT/PATCH له (=404/405) + التعديل الوحيد بعكس موثق | ✅ مختبَر |
| **قبول #5:** عجز الجرد يولّد #19 بقيمة كشف الفروقات بالهللة (مثال: 6.6000 بالضبط) | ✅ مختبَر |
| RBAC عبر HTTP: كاشير ممنوع من المخزون (403)، مشتريات لا تعتمد أمرها بنفسها (403/409) | ✅ مختبَر |
| واجهات عربية RTL: كتالوج (أصناف/تصنيفات/مستودعات/موردون+أسعار)، دورة المشتريات الخماسية، حركة المخزون (صرف/تحويل/هالك/جرد بشبكة عدّ)، تقارير وتنبيهات وسياسة قابلة للضبط | ✅ |

**الاختبارات الآن: 118/118 ✅ (46 جديداً للبوابة G5)**
تجربة حية على الخادم 2026-08-05: هجرة مخزون POS ناجحة (1210=922.00 مطابق)، تقرير
الأرصدة يطابق الأستاذ للحسابات الثلاثة (True)، فاتورة بيع REST-2026-000001 (3 برجر)
خصمت BUN/BEEF/CHEESE/FRIES بثلاث تكاليف لحظية موثقة Sale_POS وبقيت المطابقة صحيحة.

**ملاحظة انتقال البيانات:** عند ترقية قاعدة موجودة، `ensure_coa_upgrade` يضيف حساب
5120 غيابياً، و`seed_inv` يزرع وحدات المخزون ويهجّر أرصدة POS بلا إعادة محاسبتها
(القيم محفوظة بين الدفتين)، ويُنشئ مستودعاً آلياً لكل منفذ بيع.

## المرحلة 6 — الموارد البشرية والرواتب (بوابة G6) ✅
| البند (ملف 06) | الحالة |
|---|---|
| §1 الهيكل: أقسام بمركز تكلفة وحساب مصروف رواتب GL + وظائف/درجات + ملف موظف (هوية وطنية مشفرة ساكناً ADR-0010) وبدلات ثابتة | ✅ |
| §1 دورة الحياة: تعيين → تعليق → إنهاء + خطابات تغيير (ترقية/نقل/تعديل راتب) بمستند واعتماد (ذات-الاعتماد ممنوع) وأثر رجعي محمي | ✅ مختبَر |
| §2 ورديات (صباحية/مسائية/ليلية عابرة) + Roster شهري + حضور يدوي **لا يُحتسب إلا باعتماد المشرف** (غير المعتمد يُتتبّع ولا يدخل المسير) + إلغاء اعتماد محجوب بعد ترحيل الشهر | ✅ مختبَر |
| §2 إجازات: أنواع قابلة للضبط باستحقاق شهري، طلب → اعتماد → خصم رصيد آلياً (مع حجز المعلق)، منع تجاوز الرصيد، بدون أجر يخصم من المستحقات | ✅ مختبَر |
| §2 جزاءات تأديبية بمستند + اعتماد، دائنها قابل للضبط (2220 استقطاعات أو 4901 إيرادات وفق سياسة المنشأة) | ✅ مختبَر |
| §3 سلف: سقف % من الراتب مفروض على الخادم → اعتماد → صرف قيد #23 (مدين 1130 بالطرف EMPLOYEE / دائن 1101) + جدولة إقساط آلية من أول راتب، واستحالة خصم يتجاوز المتبقي | ✅ مختبَر |
| §4.1 بنود رواتب قابلة للضبط: ثابت أو % من الأساسي + خصومات قانونية بائتة افتراضياً يضبطها محاسب العميل لكل بلد (ADR-0026) + أيام شهر 30 ثابتة أو تقويمية + ساعة إضافية = أساسي/30/ساعات×مضاعف | ✅ مختبَر |
| §4.2 معالج شهري: استيراد معتمدات الشهر → مسودة بلقطة Snapshot كاملة (البنود والمدخلات) → تجهيز HR → اعتماد مالية (المعتمد ≠ المُعدّ مفروض) → ترحيل #21 ذرياً موزعاً على أقسام → قسائم → صرف #22 جماعي أو فردي بإثبات | ✅ مختبَر |
| §4.3 مسير واحد نشط لكل شهر؛ التصحيح بتسوية SUPPLEMENTAL موثقة (تظهر سطراً مستقلاً بكشف الموظف؛ الملغى يحجز رقمه)؛ مسير نهائي فوري للمنتهي بمكافأة اختيارية وتسوية كل السلف + مخصص نهاية خدمة شهري اختياري (قيد مستقل إلى 2320) | ✅ مختبَر |
| §5 تقارير: كشف شهري (قسم/موظف/بند) + مقارنة أشهر + تقادم أرصدة السلف (كشف 1130) + أرصدة الإجازات المستحقة + دوران الموظفين + كلفة الموظف الكاملة مقابل إيراد الشهر (مؤشر USALI استرشادي) | ✅ مختبَر |
| §6 الخصوصية الصفية: الرواتب تُقنَّع لمن لا يملك `hr.salary.view`، أقسام سرية 🔒 تحتاج تفويضاً صريحاً `hr.salary.confidential` (فوق RBAC)، وكل فتح قسيمة/كشف حدث مُدقَّق | ✅ مختبَر |
| **§7 قبول-1:** نتيجة المحرك = حساب يدوي موقَّع (عينة ذهبية: إجمالي 3656.2500 / صافٍ 2861.2500 بالهللة عبر بدل % وإضافي ×1.5 وغياب وتأخير وخصم بدون أجر وجزاء وقسط سلفة واستقطاع قانوني 6%) | ✅ مختبَر |
| **§7 قبول-2:** #21 متوازن وموزع على أقسام المسير وصافيه = رصيد 2310 | ✅ مختبَر + حي |
| **§7 قبول-3:** استحالة خصم سلفة فوق رصيدها؛ استحالة راتب لموظف منتهٍ؛ استحالة صافٍ سالب | ✅ مختبَر |
| **§7 قبول-4:** لا تعديل لمسير معتمد من أي مسار؛ التسوية تظهر سطراً جديداً في كشف الموظف | ✅ مختبَر |
| RBAC عبر HTTP: رؤية بلا إدارة ممنوعة من الإنشاء (403)، كشف قسم سريٍّ بلا تفويض مرفوض (403 HR.PRIVACY)، إعداد HR ≠ اعتماد مالية | ✅ مختبَر |
| واجهات عربية RTL: شؤون الموظفين (ملفات/أقسام/خطابات)، الوقت والحضور (رصد جماعي/ورديات وRoster/إجازات/جزاءات)، الرواتب (معالج + قسائم وأرشيف + سلف + بنود + سياسة)، تقارير التوظيف الستة | ✅ |

**الاختبارات الآن: 146/146 ✅ (28 جديداً للبوابة G6 — ملف 06 §7 بمعاييره الأربعة مغطى حرفياً)**
تجربة حية على الخادم 2026-08-05: مسير 2026-03 (5 موظفين، صافٍ 8250.0000) مشى
كاملاً: مسودة ← تجهيز HR ← **رفض ذات-الاعتماد (HR.SELF_APPROVAL)** ← اعتماد
مستخدم مالية مستقل ← ترحيل #21 متوازناً Dr=Cr=8250.0000 موزعاً على
6110/6210/6310 مقابل 2310 ← صرف #22 إجمالي — وصفحات الواجهة الأربع سليمة.

## المرحلة 7 — الأصول الثابتة + إقفال السنة + القوائم المالية (بوابة G7) ✅
| البند | الحالة |
|---|---|
| جداول الأصول (ملف 11 (ز) حرفياً): fa_assets (تكلفة/خردة/عمر/طريقة STRAIGHT·DECLINING/حسابات ثلاثة)، fa_depreciation_runs بلقطة سطور JSON | ✅ |
| حدث #24: تشغيلة شهرية Dr 7101/حساب مصروف الأصل | Cr 1590/مجمّعه عبر المحرك، قسط ثابت = (تكلفة−خردة)/عمر بالهللة، متناقص مضاعف بأرضية خردة، بدء من الشهر التالي للشراء، إعدام يوقف الشحن | ✅ مختبَر |
| تشغيلة واحدة لكل شهر (قيد فريد + last_run_month + مفتاح Outbox) — إعادة الشهر = 400 FA.RUN_EXISTS | ✅ مختبَر + حي |
| سجل الأصول مقابل الأستاذ: NBV السجل ≡ (حسابات الأصول − المجمعات)، بقيود اقتناء موثقة؛ والأصل بلا قيد شراء يُطفئ علم المطابقة حتى يُوثَّق | ✅ مختبَر |
| حالات الفترات بصيغتها الكاملة (02 §5): OPEN ← SOFT_CLOSED (يوقف العادي + فحص صفرية 8001) ← HARD_CLOSED (نهائي مطلقاً) + قيد تسوية للمدير المالي عبر مسار مستقل بصلاحية periods.adjust | ✅ مختبَر |
| إقفال السنة (02 §5): قائمة تحقق (1–11 مغلقة، لا مسودات، 8001 صفر، لم يُنفَّذ) ← قيد CLOSING واحد متوازن (إيرادات↔3202، 3202↔مصروفات، الصافي إلى 3201) ← سنة جديدة بـ12 فترة ← قيد OPENING بكل الميزانية **بأرصدة كل طرف** (نزيل/شركة/مورد/موظف) ← السنة القديمة CLOSED وكل فتراتها صلبة | ✅ مختبَر + قبول-7/2·7/3 |
| بنية المرساة (ADR-0031): التقارير تجمع من آخر افتتاحية عند عبور السنة — لا مضاعفة أرصدة، وميزان/أستاذ/أعمار يتسق معها كلها | ✅ مختبَر |
| قائمة دخل USALI (02 §10): دخل كل قسم (غرف/مأكولات/أخرى) ← غير موزعة ← GOP ← غير تشغيلية ← الصافي — بأرقام ذهبية موقعة | ✅ مختبَر |
| الميزانية العمومية بقطع: أصول متداولة/ثابتة/نظام = خصوم + حقوق ملكية (+ ربح العام غير المُقفل سطراً صريحاً) — balanced دائماً | ✅ مختبَر + حي |
| التدفقات النقدية غير المباشرة: هوية «تشغيلي+استثماري+تمويلي+تحويلات = Δ النقدية» محققة بالهللة كضبط مدمج | ✅ مختبَر |
| أعمار الذمم FIFO (0-30/31-60/61-90/+90) للنزلاء 1110/المدينة 1120/الموردين 2101 — مجموع الدلاء ≡ رصيد الأستاذ حرفياً | ✅ مختبَر |
| RBAC عبر HTTP: كاشير ممنوع من الأصول والقوائم (403)، تسوية الفترة اللينة حكر periods.adjust، الإقفال المزدوج 400 | ✅ مختبَر |
| واجهات RTL: الأصول الثابتة 🏭 (سجل/جدولة استشرافية/تشغيلات)، القوائم المالية 🧾 (أربع قوائم)، الإقفال والفترات 🔐 (خريطة فترات + معالج سنة بتنفيذ ذري) | ✅ |

**الاختبارات الآن: 168/168 ✅ (22 جديداً — قبول-7: قسط موقَّع 250.0000/متناقص
حتى أرضية الخردة، #24 متوازن ومرة واحدة، USALI ذهبية 7950، هوية النقدية صفر
فارق، أعمار=أستاذ، إقفال يصفّر الإيراد/المصروف و3201 يستلم 7000 بالضبط
والافتتاحية بحزبية السلف 500).**
تجربة حية 2026-08-05: 3 أصول مزروعة بقيود اقتناء افتتاحية ← تشغيلة يوليو
#24 (544.7917 لثلاثة أصول) ← السجل ≡ الأستاذ (24955.2083) ← إعادة التشغيل
مرفوضة FA.RUN_EXISTS ← القوائم الأربع تعمل والميزانية متوازنة والشاشات الثلاث 200.

**ملاحظة الطيار (G7):** قوائم قبول 03..06 كلها مختبَرة آلياً وتقارير 02 §10
مكتملة؛ يتبقى بند «توقيع محاسب ثانٍ» التشغيلي — يُوثَّق يدوياً عند أول عميل حقيقي.

---

## المرحلة 8 — الترخيص (ملف 07 كاملاً) — ✅ 2026-08-05 — 186/186 ✅

**«صارم على الترخيص، رحيم بالتشغيل» — بنية الإيراد كاملة بلا إيذاء للعميل الملتزم:**

- **سلطة التوقيع (§1):** Ed25519؛ المفتاح العام مضمَّن في الحزمة
  (`backend/app/keys/license_public.pem`)، والخاص خارج المستودع كلياً
  (`dev_keys/` مهمَل بـ git + فحص CI آلي يمنع تسربه). التحقق محلي 100% —
  لا اتصال إنترنت في مسار التحقق إطلاقاً (معيار §7-2).
- **ملف الترخيص (§1):** JSON موقَّع على الحقول كاملة (license_id، tenant،
  الباقة LOCAL/CLOUD/HYBRID، modules_enabled، max_users/branches،
  issued/expires، grace_days=30 افتراضي، features_flags، support_level،
  nonce، serial رتيب يمنع التراجع بالملفات).
- **دورة الحياة (§2):** إصدار ← تسليم ← تفعيل ببصمة جهاز بتسامح 2/3
  (لوحة أم + قرص نظام + MAC، مجزأة بملح — لا قيم خام) ← تجديد سلس
  يحل محل القديم بلا توقف ← إلغاء بقوائم موقعة متسلسلة. الوضع المعزول
  تماماً: طلب تفعيل (بصمة) → الشركة توقّع ملفاً مقيّداً بها.
- **الإيقاف الرحيم (§3):** مهلة بكامل الوظائف ← قراءة فقط (تقارير/طباعة/
  تصدير/نسخ سليمة كاملة؛ تُمنع الإضافات فقط) ← إيقاف نهائي **ليس آلياً
  أبداً** بل بوثيقة SUSPEND موقعة من الشركة، وعندها لا دخول إلا لحساب
  الطوارئ المالي للتصدير. ممنوع بتاتاً: حذف/تشفير بيانات العميل.
- **مكافحة العبث (§4):** منع الرجوع بالساعة بمرساة تاريخ رتيبة (رجوع > يوم =
  قفل احترازي بلا فقدان؛ يفك بملف إصلاح موقّع يعيد الترسيخ)، رفض فوري لأي
  تعديل ولو بحرف (§7-1 مختبَر)، اكتشاف انتقال الجهاز (بصمة أخرى = INVALID
  + علم LICENSE_TAMPER_SUSPECT)، والدفاع التجاري موثق كقرار (ADR-0032).
- **القيود الصلبة (§5):** عبر حارس `require_module` على مستوى الموجّهات
  (فندقة/POS/مخزون/HR/أصول) برسالة ترقية، وحد المستخدمين عند الإنشاء،
  والقراءة من الحمولة الموقّعة لا من قاعدة قابلة للتعديل.
- **التسجيل (§6):** كل حدث (تثبيت/تحقق/فحص دوري 6h/إلغاء/عبث مشتبه/تغيير
  حالة) في سلسلة التدقيق التجزئية المحلية.
- **الواجهة:** شاشة حالة ترخيص كاملة (/license: الحالة/الحدود بعدّادات/
  الوحدات/البصمة/التثبيت اللصقي/طلب التفعيل المعزول/قوائم الإلغاء/فحص فوري)
  + شريط تنبيه رحيم في الهيكل (أصفر مهلة، أحمر قيد، تحديث كل 5 دقائق).
- **وضع تجريبي:** تثبيت بلا ملف = TRIAL كامل المزايا 90 يوماً بمنطق المهلة
  نفسه — المعاينة لا تتعطل والإيراد محمي بعدها.

**معايير القبول §7 الخمسة كلها مختبَرة آلياً (18 اختباراً):**
حرف واحد = رفض ✓ | محلي ساري بلا إنترنت 60 يوم = صفر تعطل ✓ | انتهاء+مهلة =
قراءة فقط مع تصدير سليم (مسار HTTP كامل: TB/BS/أستاذ 200 والقيد اليدوي
403 LIC.READ_ONLY) ✓ | رجوع الساعة سنة = كشف CLOCK_LOCK ثم إصلاح بملف موقّع
بلا فقدان قيد واحد ✓ | لا مفتاح خاص في المستودع (فحص آلي) ✓.

**تجربة حية 2026-08-05:** دورة كاملة على المعاينة: TRIAL ← طلب تفعيل
معزول ← أداة الشركة `scripts/license_tool.py` وقّعت LIC-2026-0001 مقيّداً
ببصمة الجهاز ← تثبيت ناجح ← LICENSED حتى 2027-08-05 (بصمة 3/3، 6 وحدات)
← ملف معبّث (max_users=9999) مرفوض فوراً LIC.BAD_SIGNATURE ← الشاشة 200.
العرض التجريبي مرخّص الآن بترخيص سنوي حقيقي موقّع.

---

## المرحلة 9 — لوحة تحكم الشركة (ملف 08 كاملاً) — ✅ 2026-08-05 — 206/206 ✅

**تطبيق مستقل تماماً (`backend/vendor/`, منفذ 8100, قاعدة وسرّ JWT خاصان):**

- **العزل (§1):** قاعدة `vendor_dev.db` منفصلة لا تشارك جدولاً واحداً مع
  أنظمة العملاء؛ صفر ثقة عكسية — لا قيود ولا نزلاء هنا أبداً. بيئة
  `VENDOR_*` مستقلة. استخراجه لمستودع مستقل لاحقاً = نقل مجلد فقط (ADR-0033).
- **CRM (§2):** ملف عميل كامل (قانوني/تجاري/اتصال/فندق/غرف/قناة بيع/عقد/
  باقة/وحدات/تفعيل/انتهاء/حالة ستية) + سجل زمني إلزامي السبب والمستخدم
  والطابع لكل تغيير حالة — السبب الفارغ مرفوض آلياً (قبول §8-5 بلا ثغرة).
- **الفوترة (§3):** خطط أسعار (تأسيس/سنوي/شهري/وحدات/مستخدم/فرع إضافي
  وسقف خصم اعتماد — الخصم فوق السقف مرفوض) ← فواتير بتذكير 30/14/7 محسوب
  ← تحصيل يدوي موثق بمرجع إلزامي (نقد/تحويل) ← **اكتمال السداد يولّد
  ترخيصاً مجدداً آلياً ويحدّث الملف مرة واحدة** ← قواعد دورية: انتهاء بلا
  سداد ← GRACE + مهمة مبيعات ثم READ_ONLY ذاتياً.
- **التراخيص (§4):** إصدار/تجديد/تعديل/إيقاف/إلغاء بمفتاح الشركة نفسه
  (ملف 07)؛ تجاوز حدود الخطة ← PENDING_APPROVAL ولا اعتماد ذاتياً
  (VND.SELF_APPROVAL) — مدير ثانٍ فقط يفعّله؛ قوائم إلغاء موقعة متسلسلة
  تصل العملاء عبر Edge؛ سجل أعلام بصمة الأجهزة الغريبة.
- **الدعم (§5):** تذاكر (تصنيف/أولوية P1..P4/وحدة/قناة/مرفقات/SLA محسوب
  ‏= 4/24/72/168 ساعة) بسير العمل الستي حرفياً؛ سبب جذري إلزامي لإغلاق
  الأعطال (TKT.ROOT_CAUSE_REQUIRED)؛ تصعيد L1←L2←هندسة؛ فتح من داخل
  تطبيق العميل عبر Edge (بلا P1 — للشركة فقط).
- **صحة الأسطول (§6):** نبض (اتصال/نسخة/تراكم Outbox/نسخ احتياطي/قرص)
  بمؤشر موحد أخضر/أصفر/أحمر/بلا-نبض؛ حلقات تحديث Canary ← عام بسجل
  نجاح/فشل لكل عميل؛ علم «بصمة على Tenant آخر» من 07 §4-5.
- **أمن اللوحة (§7):** MFA إلزامي لكل مستخدم بلا استثناء (تسجيل إجباري
  أول دخول + رفض API قبل الإكمال VND.MFA_ENROLL_REQUIRED)؛ أقل امتياز
  بمصفوفة أدوار (الدعم لا يصدر تراخيصاً، المالية لا تعمل التذاكر)؛
  تدقيق مستقل مسلسل التجزئة على كل فعل حساس.
- **واجهة العميل المحدودة (§1):** `/edge/*` برمز Edge لكل عميل — نبض +
  أحدث ترخيص موقّع + أحدث قائمة إلغاء + فتح تذكرة + أحدث إصدار وإقراره.
  نقطة. رمز العميل لا يفتح أي شيء آخر (مختبَر سلبياً).

**معايير القبول §8 الخمسة مختبَرة آلياً (20 اختباراً):**
سداد تجديد ← ترخيص جديد يصل عبر Edge فوراً ويوثَّق توقيعه بالمفتاح العام
المضمَّن ✓ | MRR = مجموع النشطة حسابياً (358.3333/558.3333 بدقة Decimal
وبإثبات تفصيلي) ✓ | P1 ← إشعار مناوب بتسليم < 60 ثانية موثق بالطوابع ✓ |
دعم لا يصدر ترخيصاً (403) ✓ | كل تغيير حالة بسبب+مستخدم+طابع ✓.

**تجربة حية 2026-08-05 (localhost:8100):** دخول MFA بمدير الشركة ← MRR
358.3333/ARR 4299.9996 ← فاتورة INV-2026-0001 (3700.0000) ← تحصيل كامل
بمرجع TRX-DEMO-001 ← ترخيص LIC-CLI-001 S1 تولّد آلياً ← سُحب عبر Edge
وتوقيعه صالح بالمفتاح العام للعميل ← تذكرة TKT-2026-00001 (P1) وأُشعر
المناوب بنفس الثانية ← نبض CLI-001 أخضر وCLI-002 بلا نبض.

**ملاحظة تأجيل واعٍ:** إرسال النبض المجدول من تطبيق العميل يُربط بمجدول
المهام في G10 (محرك المزامنة)؛ استقبال النبض وتلوينه مختبَر وجاهز الآن.

## المراحل القادمة (حسب ملف 15)
- [ ] محرك المزامنة الهجينة (09) — G10
- [ ] التصليب الأمني الكامل (08/13) + النسخ الاحتياطي — G11
- [ ] الإطلاق التجريبي الحقيقي — G12

## كيفية التشغيل
```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000
# الدخول التجريبي: admin / admin123!Change  (بيئة تطوير فقط)
```
الاختبارات: `cd backend && python3 -m pytest`

## ملاحظة تشغيلية — CI
ملف GitHub Actions جاهز في `.github-ready/workflows/ci.yml` ولكن **غير مفعَّل**:
توكن GitHub App للجلسة لا يملك صلاحية `workflows` فيرفض الخادم رفع أي ملف داخل
`.github/workflows/`. التفعيل يدوياً من واجهة GitHub (نسخ الملف) أو بصلاحية أوسع.
