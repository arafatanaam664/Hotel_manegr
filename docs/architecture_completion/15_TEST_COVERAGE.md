# 15 — خارطة الاختبارات النهائية (Test Coverage Completion)

> **المرحلة الثانية — حصر وتصميم فقط؛ لا اختبار يُكتب هنا.**
> الواقع معدود آلياً: **271 دالة اختبار في 14 ملفاً** (`test_accounting_core/auth/fin/g1_month/hotel/hr/inventory/license/police/pos/suites/sync/users/vendor`) — كلها سلوكية خلفية: وحدات دومين + تكامل API + أمن أساسي + مزامنة + ترخيص.

## A. فجوة التغطية بالأنواع — الواقع × المستهدف
| النوع | موجود اليوم | المستهدف النهائي | ملاحظات |
|---|---|---|---|
| Unit/Domain | ~ضمن الـ271 | يُستكمل لكل قاعدة عمل جديدة | المعيار: كل سطر «قاعدة مالية/حسابية» له اختبار حواف |
| Integration/API | ✅ الأساس | +230 لواجهات كتالوج 12 الجديدة | عقد: نجاح/صلاحية/تحقق/حدث تدقيق لكل نقطة |
| UI/E2E | **0** | +120 (Playwright RTL) | الرحلات الحاكمة بند C |
| Security | جزئي (قفل/صلاحيات/تدقيق) | +40 | مصفوفة RBAC آلية كاملة 121 صلاحية × أدوار |
| Performance | **0** | +15 | ميزانيات كتالوج 14-E تُقاس |
| Load | **0** | +10 | سيناريوهات الذروة بند D |
| Recovery/DR | **0** | +15 | مع محرك P0-3: استعادة آلية كبوابة إصدار |
| Offline | ضمني بنيوي | +12 | عزل الكابل: يوم كامل تشغيل دون شبكة |
| Sync | ✅ أساسي (round-trip/تعارض) | +30 | فجوات/تكرارات/chaos 5% فقد/Reseed/USB/توسعة P0-2 بكل كيان |
| Accounting (ذهبية) | ✅ G1 شهر موقَّع | +30 | خريطة 27 حدثاً ذهبية + فوليو=دفتر + أعمار |
| Regression/إصدار | جزئي | +10 بوابات | صحة/بذر/ترخيص/ترقية قاعدة additive/E2E نموذجي |
| **الإجمالي** | **271** | **≈ 1,183** | **+≈912 موزعة على P0..P4** |

> تصالُح مع «670» في إحصائية المرحلة الأولى: كانت تقيس **السلوك الخلفي لنطاق P0–P2 فقط**. هذا الجدول برنامج المنظومة الكامل (أنواع اختبار جديدة كلياً لم تكن موجودة).

## B. الرحلات الحاكمة لاختبارات E2E (Playwright — RTL صريح)
1. تسكين كامل ≤60ث من لوحة DSH-05 (يُقاس زمنياً آلياً!) · 2. حجز→عربون→تنسّق بالمعلومية · 3. بيع POS→KDS→تسديد→شحن غرفة→يظهر بالفحص · 4. دورة شراء كاملة إلى سداد بمطابقة ثلاثية ناجحة وفاشلة · 5. مسير رواتب بفصل ثلاثي وقسيمة سرّية · 6. إقفال يوم→لقطة→تقرير إشغال · 7. إقفال فترة لين→تسوية→صلب · 8. إنشاء مستخدم بنوافذ دوام→دخول/منع خارج النافذة · 9. MFA إلزامي لدور مالك (P0-4) · 10. نسخ/استعادة بموافقة مزدوجة · 11. رفع مرفق وعرضه بصلاحية · 12. بحث شامل يقفز لحجز · 13–20: مجموعة/انتظار/ميني بار/موازنة×فعلي/كشف طرف/أمر صيانة/لوحة صحة/معالج إعداد أول.
(20 رحلة أساسية × تحققات = نواة الـ120؛ توسعها مع المراحل.)

## C. سيناريوهات الحمل (Load) الملزمة
| السيناريو | الهدف |
|---|---|
| 40 تسكيناً متزامناً على 20 غرفة | استحالة البيع المزدوج تحت الضغط (قبول ملف 03 رقم 1) |
| 5–15 ألف حدث مزامنة على محاكي فقد 5% | < 5 دقائق (نص ملف 09 §3) |
| Tape Chart على فندق 400 غرفة×90 يوم | عرض تفاعلي ≤300مـس استعلام |
| تقرير إشغال سنة كاملة | ≤8ث أو ينزل خلفياً بإشعار (قرار ENG-RPT) |
| 200 مستخدم نشط متزامن موقع واحد | p95 API ≤300مـس |
| نسخة احتياطية أثناء الذروة | لا تجميد عمليات >1ث |

## D. بوابات الإصدار الإلزامية (Definition of Done للأتمتة)
أخضر للفروع: كل الـ271 + الجديدة بمرحلتها · لا إصدار مع E2E أحمر في الرحلات 1/6/10 · تقرير تغطية منشور بالنسب لكل وحدة (هدف ≥85% سلوك قلبي) · فحص أسرار تلقائي (لا تسرّب) · فحص ترقية قاعدة من آخر إصدار مُنتج (additive) · تشغيل restore-drill أسبوعياً ضد نسخة حقيقية.

سجل التغييرات: v1.0 (2026-08-07) — فريق العمارة الاستشارية.
